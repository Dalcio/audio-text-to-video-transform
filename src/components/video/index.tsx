import React from 'react';
import { VideoPlayerContainer } from './styled';

export const VideoPlayer: React.FC = () => {
    return (
        <VideoPlayerContainer>Hello from Video container</VideoPlayerContainer>
    );
};
